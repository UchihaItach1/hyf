server:
  port: 9863
  
redis:
  cache:
    expiration: 3601

wlj:
  auth:
    clientId: wlj
    clientSecret: wlj
    scope: server

urls:
  anon:
    - /authentication/**
    - /**/*.css
    - /social
    - /signin
    - /signup
    - /info
    - /health
    - /metrics/**
    - /loggers/**
    - /mobile/token
    - /admin/common/**

logging:
  config: classpath:logback.xml

